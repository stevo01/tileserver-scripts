
services:
  osmdbutils:
    image: osmdbutils
    build:
      context: ./src
    container_name: osm-db-utils
    hostname: osmdbutils
    mem_limit: 96g
    restart: "no"
    command: run
    volumes:
      - ./volumes/openstreetmap-flat-nodes:/nodes
      - ./volumes/transfer:/transfer
      - ./volumes/work:/replication/work
      - ./volumes/openstreetmap-downloads:/replication/download
      - ./volumes/backup:/backup
    networks:
      - tile-postgis-network

networks:
  tile-postgis-network:
    external: true
  
# docker network create tile-postgis-network